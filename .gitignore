# 构建产物
dist/
dist-ssr/
dev-dist/
# 重建时的临时备份目录（如 dist_bak_*），非源码、不应提交
dist_bak*/

# 依赖
node_modules/
.pnp
.pnp.js

# 本地环境变量
.env
.env.local
.env.*.local

# 编辑器
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 系统文件
.DS_Store
Thumbs.db

# 日志
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Trae 临时文档(本地设计稿,不需要上传)
.trae/

# 教材 PDF 原件(体积大,不纳入版本控制)
src/data/starlight0/
src/data/starlight0_extracted.json

# 本地构建缓存
.vite/

# 本地 agent 工作数据（非应用源码）
.workbuddy/
/src/data/rocketgirl

# Playwright 测试产物（每次运行生成，不纳入版本控制）
test-results/
playwright-report/

# ===== 工作产出：临时脚本 / 备份 / 验证产物 / 原始素材 =====
# 构建验证产物（非源码，沙箱 safe-delete 拦截时用 --outDir dist-verify 验证）
dist-verify/
dist-probe*/

# 页面截图/对比图验证产物（Playwright 临时验证脚本 + 输出目录，非源码）
.cache/
scripts_shot*.mjs


# 源文件备份
*.bak
*.bak[0-9]*

# 本轮工作脚本（数据处理/翻译/PDF 抽取等草稿，非应用源码）
scripts_*.py
gen_dict*.py
_gen_dict*.py
scripts_dict_words.txt
scripts_still_missing.json
scripts_trans/
scripts_pdf_out/
# Starlight 课本提取/翻译/验证脚本与产物（本地用，不提交；生成的 starlight-book.ts 已纳入版本控制）
scripts_sl_*.json
scripts_sl_*.txt
dist-final*/

# 原始绘本素材（含版权 PDF/txt，不纳入版本控制）
src/data/Fly Guy/

